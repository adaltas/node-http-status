// Generated by CoffeeScript 2.3.0
var fs, key, path, statuses;

fs = require('fs');

path = require('path');

statuses = require('./index');

fs.writeFile(path.join(__dirname, '../lib/index.d.ts'), 'export = {\n' + ((function() {
  var i, len, ref, results;
  ref = Object.keys(statuses);
  results = [];
  for (i = 0, len = ref.length; i < len; i++) {
    key = ref[i];
    results.push(`\t${JSON.stringify(key)}: ${JSON.stringify(statuses[key])},\n`);
  }
  return results;
})()).join('') + '}\n', function(err) {
  if (err) {
    throw new Error(err);
  }
});
